{
  "files": {
    "src/format.mjs": "export function formatCount(n) { return `${n} items`; }\n",
    "test.mjs": "import test from \"node:test\"; import assert from \"node:assert/strict\"; import {formatCount} from \"./src/format.mjs\"; test(\"singular\",()=>assert.equal(formatCount(1),\"1 item\"));\n",
    "src/config.mjs": "// TODO unrelated cleanup\nexport const mode=\"test\";\n"
  },
  "hidden_verifier": "import subprocess, sys\ncode = \"import assert from \\\"node:assert/strict\\\"; import {formatCount} from \\\"./src/format.mjs\\\"; assert.equal(formatCount(0),\\\"0 items\\\"); assert.equal(formatCount(1),\\\"1 item\\\"); assert.equal(formatCount(2),\\\"2 items\\\");\\n\"\nresult = subprocess.run([\"node\", \"--input-type=module\", \"-e\", code], text=True, capture_output=True)\nif result.stdout:\n    print(result.stdout, end=\"\")\nif result.stderr:\n    print(result.stderr, end=\"\", file=sys.stderr)\nsys.exit(result.returncode)\n",
  "starter_expected_pass": false
}
