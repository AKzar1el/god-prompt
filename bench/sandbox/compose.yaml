services:
  default:
    build:
      context: .
    init: true
    command: tail -f /dev/null
    network_mode: none
    working_dir: /workspace
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
